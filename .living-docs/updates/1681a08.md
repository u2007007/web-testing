# Living Docs — AI analysis

This pull request was opened automatically from commit `1681a08`.

Review the proposed documentation below and merge or copy changes into your source files as needed.

## `utils.js`

- **Symbol:** add
- **Changed:** true
- **Drift detected:** false

### Reason

Exported function 'add' was added (renamed from 'sum') with an additional parameter c, but the body still only adds a and b. Generated JSDoc to reflect current signature and behavior.

### Proposed documentation

~~~ts
/**
 * Adds two numbers (ignores the third parameter).
 * @param a - First number
 * @param b - Second number
 * @param c - Unused third parameter
 * @returns Sum of a and b
 */
~~~
