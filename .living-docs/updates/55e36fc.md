# Living Docs — AI analysis

This pull request was opened automatically from commit `55e36fc`.

Review the proposed documentation below and merge or copy changes into your source files as needed.

## `utils.js`

- **Symbol:** add
- **Changed:** true
- **Drift detected:** true

### Reason

The function signature changed from three parameters (a, b, c) to two (a, b), and the body now returns a + b instead of a + b + c. The stored documentation still describes three parameters and a sum of three numbers, which is now inaccurate. The inline JSDoc visible in the diff appears to already reflect the two-parameter version, but the stored doc does not.

### Proposed documentation

~~~ts
/**
 * Adds two numbers together.
 * @param a - First number
 * @param b - Second number
 * @returns Sum of a and b
 */
~~~
