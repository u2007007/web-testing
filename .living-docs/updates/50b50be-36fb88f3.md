# Living Docs — AI analysis

This pull request was opened automatically from commit `50b50be`.

Review the proposed documentation below and merge or copy changes into your source files as needed.

## `utils.js`

- **Symbol:** add
- **Changed:** true
- **Drift detected:** true

### Reason

The function signature was changed to accept three parameters (a, b, c), but the body still returns only a + b, ignoring c. The existing documentation claims it adds three numbers and returns the sum of a, b, and c, which no longer matches the actual behavior.

### Proposed documentation

~~~ts
/**
 * Adds two numbers together, ignoring the third.
 * @param a - The first number.
 * @param b - The second number.
 * @param c - The third number (unused).
 * @returns The sum of a and b.
 */
~~~
