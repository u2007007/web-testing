# Living Docs — AI analysis

This pull request was opened automatically from commit `81a44f2`.

Review the proposed documentation below and merge or copy changes into your source files as needed.

## `utils.js`

- **Symbol:** add
- **Changed:** true
- **Drift detected:** true

### Reason

The function now returns a + b + c instead of a + b, so it adds three numbers. The existing documentation incorrectly states it adds two numbers and that c is unused, and the return description is incomplete.

### Proposed documentation

~~~ts
/**
 * Adds three numbers.
 * @param a - First number
 * @param b - Second number
 * @param c - Third number
 * @returns Sum of a, b, and c
 */
~~~
