# Living Docs — AI analysis

This pull request was opened automatically from commit `6396e3c`.

Review the proposed documentation below and merge or copy changes into your source files as needed.

## `utils.js`

- **Symbol:** add
- **Changed:** true
- **Drift detected:** true

### Reason

The function signature changed from `add(a: number, b: number, c: number)` to `add(a: number)`, removing parameters `b` and `c`. The existing documentation still describes adding two numbers, which is no longer accurate because the function now only accepts one parameter and relies on an external `b`.

### Proposed documentation

~~~ts
/**
 * Adds a number to an external variable `b`.
 * @param a - The number to add.
 * @returns The sum of `a` and `b`.
 */
~~~
